<h1><%= @cocktail.name %></h1>
<ul> <% @cocktail.doses.each do |dose| %>
 <li><%= dose.ingredient.name %> - <%= dose.description %> <%= link_to "Delete", dose_path(dose),
      method: :delete,
      data: { confirm: "Are you sure?" } %></li>
<% end %>
<%= link_to "add another ingredient" ,new_cocktail_dose_path(@cocktail)  %>

</ul>
<%= link_to "back to list", root_path, class: 'btn btn-primary' %>
<%= link_to "create a new cocktail", new_cocktail_path, class: 'btn btn-success' %>
